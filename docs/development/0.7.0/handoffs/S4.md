# S4 handoff — windows, estimators, cycles (A3–A5)

| | |
|---|---|
| session | S4, branch `work/0.7.0-execution-analysis` |
| base / contract | `fabbb4a` (contract), branch base `3fe32b8`; S0 rulings cherry-picked as `b27b190` (from `4d98589`) |
| current commit | see `git log` — the commits are listed below |
| acceptance matrix | [S4-acceptance-matrix.md](S4-acceptance-matrix.md), written before any long campaign |
| milestone state (S4's view; S0 owns STATUS) | A3: IMPLEMENTED on analytic fixtures; molecular hydration BLOCKED on A1/A2. A4, A5: cycle arithmetic, restraints and standard state IMPLEMENTED; molecular legs BLOCKED on A1/A2 and GPU |

## What exists

| file | what it is |
|---|---|
| `src/md_tools/alchemy/paths.py` | named lambda components on a piecewise-linear path with explicit orientation; `lambda` and `tau` refused as names; a window set missing a knot refused. **Now an S0 contract file, frozen at `9d465a5`** — I have not changed it since |
| `src/md_tools/alchemy/samples.py` | `md-tools-alchemical-samples/1`, adopted by S0 as THE shared sample record. Potentials at every state (kJ/mol), volume under NPT, derivative components; u = β(U + pV) is computed, never stored; `origin_state` is the only provenance and a rank/worker/device field is refused by name |
| `src/md_tools/alchemy/estimators.py` | EXP (stable log-mean-exp, Kish ESS), BAR (bracketed bisection), MBAR (pymbar 4, overlap matrix), TI (complete dU/ds segment by segment, one-sided slopes at knots, cubic-spline quadrature indicator), decorrelation, and `agreement_gate` (0.5 kcal/mol AND 3σ_c; INCONCLUSIVE above σ_c = 0.25 kcal/mol) |
| `src/md_tools/alchemy/restraints.py` | Boresch restraint as `lambda_restraints · U` (CustomCompoundBondForce, periodic when the System is), collinear anchors refused, ΔG_release to 1 M by separable quadrature with Boresch's closed form alongside |
| `src/md_tools/alchemy/cycles.py` | absolute and relative hydration, relative binding, standard absolute binding; explicit signs and orientation; refusals by name for a missing leg, wrong environment, reversed leg, mixed schemes or temperatures, missing standard-state term, release term for a different restraint |
| `src/md_tools/alchemy/windows.py` | cross-state evaluation on a separate Context, the sample-stream reporter, `ParametricHamiltonian`, `ComposedHamiltonian` (restraint in force group 16), and `run_window` over `preflight_stage` + `commit_generation` |
| `tests/test_alchemy_{estimators,cycles,windows}.py`, `tests/data/alchemy_s4/` | tests; S4's own analytic fixture `harmonic_staged_v1.json` (NOT the shared §7 fixture) and its generator |

### Interfaces other sessions consume

- **Hamiltonian (agreed with S3, 2026-09-19):** `system`, `parameter_names`, `set_state(context, state)`, `derivatives(context, state)`, `context_parameters(state)`. S4 never calls `setParameter` on a Hamiltonian's names. `lambda_restraints` is S4's; S3's Hamiltonian is handed its three names only. S3 uses force groups 0–9 and S4 group 16.
- **A window's outputs** (`window_paths`): `<id>.samples.csv` (the stream), `<id>.log` (the machine record: identity, fingerprint, every state's Context parameters, acceleration), `<id>.out` (progress for a person), `<id>.xml` (final State), `<id>.checkpoints/` (generation tree), `<id>.complete.json` (sha256 of the stream and restart). `read_window_samples(csv, record)` returns a `SampleSet`.

### Reused, not re-implemented

`preflight_stage` (flags, inputs, collisions, machine configuration, platform policy with a real Context, placement, refusal of a plural launch, timestep/HMR, ensemble), `check_output_collisions` / `check_existing_outputs`, `commit_generation` / `read_committed`, `cv.prefix.prefix_digest` / `truncate`, `openmm.seeds`, `md._stages.add_barostat`.

## Evidence

Platform for everything below: **CPU / Reference only**, `CUDA_VISIBLE_DEVICES=""`. The window
tests pass an explicit `--cpu` (`platform_selection: cli-override`). This is **not CUDA evidence**.
The interpreter is `openmm-env` (OpenMM 8.6, pymbar 4.2.0, numpy 2.4.6, scipy 1.18.0). `md_tools`
is imported from this checkout's `src/` because `tests/conftest.py` pins it there; the installed
0.5.4 package has no `alchemy`.

```bash
CUDA_VISIBLE_DEVICES="" python -m pytest tests/test_alchemy_estimators.py tests/test_alchemy_cycles.py \
    tests/test_alchemy_windows.py -q -p no:cacheprovider -m "not slow"
# -> 67 passed, 1 deselected (the slow campaign)
CUDA_VISIBLE_DEVICES="" python -m pytest tests/test_alchemy_windows.py -q -s -p no:cacheprovider -m slow
# -> W7, below
```

Guards shown to fail when the code is broken (mutation, then restored):
- TI with a single slope at the knot → `test_ti_uses_one_sided_slopes_at_a_knot` fails.
- BAR with the wrong M sign → the exact-value gate and the pymbar comparison fail.
- The release quadrature without the r² Jacobian, or without sin θ → both release tests fail.
- The resume with no truncation → the byte-identical resume test fails.
- The continuation with no prefix validation → the edited-row refusal test fails.

Numbers from the frozen analytic fixture (exact ΔG = 10.187 kJ/mol = 2.4347 kcal/mol):
MBAR 10.378 ± 0.154, BAR 10.309 ± 0.200, EXP forward 10.410 ± 0.265, EXP reverse 10.228 ± 0.786,
and TI 10.871 ± 0.255 kJ/mol, with a quadrature discrepancy of 0.414 kJ/mol. TI's trapezoid bias
on this grid is 0.56 kJ/mol analytically; it passes the gate only with σ_int included, which is
the behaviour the aims require. Minimum neighbour overlap is 0.152.

### W7: five windows × 100 000 steps, OpenMM harmonic model (18 coordinates), CPU `--cpu`

Result: **FAIL (partial)**. Exact ΔG = 8.6331 kcal/mol. 501 samples per window, 13 min 50 s.

| estimator | ΔG, kcal/mol | gate |
|---|---|---|
| MBAR | 8.6546 ± 0.1307 | PASS |
| BAR | 8.5742 ± 0.1252 | PASS |
| EXP forward | 8.7694 ± 0.2479 | PASS, though flagged `poor_overlap` |
| EXP reverse | 7.8234 ± 0.2334 | **FAIL**: off by 0.81 kcal/mol, flagged `poor_overlap` (Kish ESS 24 at w003→w002) |
| TI | 9.3733 ± 0.1420 | **INCONCLUSIVE**: quadrature discrepancy 2.59 kJ/mol → σ_int = 0.62 kcal/mol; the cubic-spline value is 8.755 |

The minimum MBAR neighbour overlap is 0.097 (w002/w003), and the EXP forward/reverse gap is
1.59 kT. Five windows are too coarse for 18 coordinates: the expected trapezoid bias is 6× the
3-coordinate fixture's, and EXP from the stiff side is the known poor-overlap direction. The gate
was not changed. The test asserts what held: MBAR, BAR and EXP forward pass, and neither failure
is silent (W7b). **W8**, with 17 windows and the same gate for all five estimators, was defined
before it was run:

### W8: 17 windows × 100 000 steps, same model and gate, CPU `--cpu` — **PASS**

| estimator | ΔG, kcal/mol (exact 8.6331) | gate |
|---|---|---|
| MBAR | 8.5791 ± 0.0677 | PASS |
| BAR | 8.5952 ± 0.0627 | PASS |
| EXP forward | 8.5910 ± 0.0969 | PASS |
| EXP reverse | 8.5460 ± 0.0967 | PASS |
| TI (σ_int included) | 8.6462 ± 0.0813 | PASS |

Command: `CUDA_VISIBLE_DEVICES="" PYMBAR_DISABLE_JAX=true python -m pytest tests/test_alchemy_windows.py -q -s -m slow -k w8` (36 min).

### N1: NPT, periodic PME water, barostat, one window resumed, CPU `--cpu` — **PASS**

Two dummy particles on a λ-dependent bond in 1 038 atoms of TIP3P, 5 windows × 10 000 steps,
201 samples per window, 5 min 48 s. Exact ΔG = 2.4347 kcal/mol. MBAR 2.4621 ± 0.1627 and BAR
2.3281 ± 0.0793 PASS; these two are the asserted rows. EXP forward 2.3062 ± 0.0936, EXP reverse
2.2138 ± 0.1545 and TI 2.4311 ± 0.0975 are reported but not asserted. u = β(U + pV) holds to a
relative 1e-12, and the box volume moved. The first run failed before reaching the gate, on the
test's own cancellation (u − U/kT at a relative 1e-12). The check now compares u with U/kT + pV/kT;
that changed an arithmetic assertion, not a physics tolerance.

### G1: the window runtime on CUDA — **PASS** at `d65d4f0`

Card 4 (RTX 3080), allocated by MD-tools-0.6.0 and released afterwards; CUDA mixed precision;
`CUDA_VISIBLE_DEVICES=4 PYMBAR_DISABLE_JAX=true python -m pytest tests/test_alchemy_windows_cuda.py -q -s -rs --error-on-skip`.
5 passed, 0 skipped, 2 min 42 s. The record says `resolved_platform: CUDA`,
`platform_selection: machine-config` and `explicit_cpu: false`, with a machine configuration
written into the test's own temp directory.

- Interrupt and resume on CUDA: the step grid and row count match, and for this model the
  stream was **byte-identical** to the uninterrupted run. That is recorded, not asserted; it may
  not hold for a large PME system.
- Read-only refusals of an edited committed row and of a changed definition hold on CUDA.
- W8 on CUDA, exact 8.6331 kcal/mol: MBAR 8.5964 ± 0.0656, BAR 8.6097 ± 0.0655, EXP forward
  8.6117 ± 0.0937, EXP reverse 8.6948 ± 0.1252, TI 8.6666 ± 0.0904. All PASS.
- N1 on CUDA, exact 2.4347: MBAR 2.2597 ± 0.1362 and BAR 2.6126 ± 0.0954 PASS; these are the
  asserted rows. Also reported, not asserted: EXP forward 2.6583 ± 0.1146, EXP reverse
  2.4425 ± 0.1805, TI 2.7228 ± 0.1190 (2.4σ from exact).

**What failed first, and what was changed.** The first G1 run (at `6d92976`) failed twice:
1. The test asserted an acceleration-record key `platform` that does not exist; the key is
   `resolved_platform`. This was a test defect.
2. The NPT test tripped the evaluation self-check: 0.0155 kJ/mol out of −13 656 (1.1e-6 relative)
   against a bound of 1e-3 + 1e-6·|E|. On the same card I measured the noise. Two
   mixed-precision Contexts at identical positions differ by up to 1.8e-6 relative (max 2.0e-2
   kJ/mol over 200 frames), and a single Context re-evaluated at the same positions moves by
   1.1e-3; in double precision both agree to 1e-10. The bound had been fixed before any
   comparison but never measured. Mixed/single now use 2e-5 relative, double/Reference keep
   1e-6, the completion record carries the bound, and a test still refuses a 0.5 kJ/mol
   disagreement. **This is a recalibration after a failure**, justified by those measurements
   and stated as one. S0 accepted it, and asked for two follow-ups:
   - **Absolute size on N1:** |E| is about 13 700 kJ/mol, so the bound allows
     1e-3 + 2e-5 × 13 700 ≈ **0.275 kJ/mol ≈ 0.110 kT** at 300 K (kT = 2.494 kJ/mol). The
     observed failure was 0.0155 kJ/mol and the measured maximum was 0.020 kJ/mol. The
     0.5 kJ/mol disagreement the test injects is 1.8× the bound. The allowance grows with the
     system: at |E| ≈ 10⁶ kJ/mol (a large solvated protein) it is about 20 kJ/mol. The check
     then only catches gross mismatches, which is its purpose; System identity is enforced by
     serialisation equality.
   - An unknown or absent GPU Precision is now **refused**, not mapped to the loosest bound.

## M2: relative hydration ethane → chloroethane — STOPPED

The rows (M2.0–M2.6) were written before any sampling (`0b27f18`). The CPU pilot, which is not
evidence, changed only what M2.0 allows: two vacuum windows, where overlap was 0.026 (`0a4ca66`).
Production began at `5c15d89` on CUDA cards 0 and 1, granted by the user through S0, at 13:44 on
2026-09-19. Both cards were checked idle first; one process ran per card, pinned, with
`--error-on-skip`. Before any window ran, S2's `matched_legs` passed for vacuum-v1 against
ethane-tip3p (ligand Hamiltonian `622bb1d6…`).

It **stopped at 13:59**. Every water repeat failed with OpenMM's "The periodic box size has
decreased to less than twice the nonbonded cutoff". The completed water windows recorded a mean
volume of 6.33 nm³, a 1.850 nm edge. That is only 2.8% above 2 × 0.9 nm, and 1 ns NPT windows
fluctuate down to 5.91 nm³ (1.808 nm). **The ethane-tip3p fixture box (1.9 nm) is too small for
NPT at a 0.9 nm cutoff.** Short CPU pilot windows never reached that tail. The vacuum legs ran
cleanly: vacuum r1 completed all 18 windows. Card 1 was stopped only because the grant's
condition is to stop and report on a failure. No protocol change was made. Both cards were
released at 13:59, and the options (a larger fixture, recommended; NVT; a shorter cutoff, not
advised) went to S0. M2.1–M2.5 remain NOT RUN.

## Incidents

- **An S4 test run held a context on all nine GPUs** (18 GB on card 0, which belongs to
  TarFlow). pymbar 4 imports JAX, and JAX preallocates on every visible device. MD-tools-0.6.0
  noticed it. Fixed in `d80a0bd`: `PYMBAR_DISABLE_JAX` is pymbar's own switch, and it changes
  nothing for any other JAX user. MBAR records which backend ran. Every S4 run since then uses
  `CUDA_VISIBLE_DEVICES=""`. MD-tools-0.6.0 had pointed out that a first version, setting
  `JAX_PLATFORMS=cpu`, was a process-wide device decision made outside `platform_policy`, so it
  was replaced.

## For S0 (files S4 does not own)

1. **Add `alchemy/windows.py` to the owner table** in shared-contracts.md (S4, per the S4
   assignment). It is not listed there.
2. **An `alchemy` optional extra** in `pyproject.toml`: `pymbar>=4,<5` and `scipy`. Both are
   imported lazily. EXP, BAR, the sample record and the cycles need numpy only, and an install
   without the extra keeps working.
3. **Public entry.** `run_window` is a callable runtime, not yet a `md-run` protocol or a
   `build-md` workflow. Wiring (`protocol: alchemical`, a window configuration schema, generated
   `window.py` entry points calling a `window_main`) is S0's CLI/schema territory. An alternative
   to decide at integration: fold the sample-stream reporter into `stage_main` as a reporter, so
   a window IS a cMD stage with one more stream. The reporter and the prefix record are written to
   allow either.
4. **Registration.** A window campaign needs a data-contract method entry before it can be
   registered. Per the 2026-09-19 rule, S4 does not touch `$MD_DATA`. A sandboxed registration
   test will run against a temporary root once the method entry exists; the real row is BLOCKED.
5. `paths.py` is frozen as S4 wrote it. One request, for when S3's Amber18 one-step path needs
   it: a helper that builds a path moving several components together over one segment.
   `linear_path` already does this for 0 → 1, so nothing is needed yet.

## Known limitations (not claims)

- NPT on CUDA is shown only on a 1 038-atom box (G1). A large solvated protein is not tested.
- **CUDA resume exactness is unknown.** CPU resume is byte-identical (W4). On CUDA it may be a
  new realisation, as AIS's is; that is to be measured, not assumed (row G1, BLOCKED).
- **Windows are independent.** There is no Hamiltonian replica exchange between windows. If it
  is added, it goes through `md_tools.remd` and its MPI authority.
- **The pairwise BAR path total** sums neighbour variances and ignores the correlation through
  shared windows. This is stated in the result.
- **Legs in a cycle are assumed independent** (separate simulations). The analytic release term
  carries zero variance, being quadrature.

## Next dependent tasks

1. When S3's molecular fixture lands: windows over `AlchemicalHamiltonian` (and
   `ComposedHamiltonian` with a restraint), with the evaluation/sampling self-check on a real
   PME system. Generate the shared frozen-energy fixture in the `md-tools-alchemical-samples/1`
   shape from it, at S0's direction.
2. When S2's plan lands: absolute hydration of a small neutral molecule (M1), then the relative
   hydration pair (M2), on CUDA, with a card allocated by MD-tools-0.6.0 or S0.
3. Relative binding (M3) and Boresch absolute binding (M4): anchor selection from the complex,
   the restraint-attachment leg (`lambda_restraints` 0 → 1 with the ligand coupled), the
   decoupling leg with the restraint on, and the release term.

## CUDA coverage inventory (for S0's `integration:` commit)

`tests/test_cuda_coverage_matrix.py` is marked gpu+slow, so no CPU lane runs it. Its two
inventory functions were called directly (importlib, `CUDA_VISIBLE_DEVICES=""`) at `15786b0`.
The only unclassified sites are in `alchemy/windows.py`. `samples`, `estimators`, `cycles` and
`restraints` have none: `BoreschRestraint.openmm_force` only builds a Force.

| site | operations | proposed class |
|---|---|---|
| `alchemy/windows.py::run_window` | construct, integrate, parameters, evaluate, checkpoint | CUDA_SITES. The platform comes only from `preflight_stage`'s `PlatformResolution` (`checked.acceleration`) |
| `alchemy/windows.py::run_window.commit` | checkpoint | CUDA_SITES (via `commit_generation`) |
| `alchemy/windows.py::CrossStateEvaluator.__init__` | construct | CUDA_SITES. It takes the Platform object and properties the caller resolved; `run_window` passes the preflight's |
| `alchemy/windows.py::CrossStateEvaluator.evaluate` | parameters, evaluate, derive | CUDA_SITES |
| `alchemy/windows.py::SampleStreamReporter.report` | derive (reads the State it is given) | CUDA_SITES |
| `alchemy/windows.py::_set_initial_coordinates` | parameters, checkpoint (setState) | CUDA_SITES |
| `alchemy/windows.py::{Parametric,Composed}Hamiltonian.{set_state,derivatives}` | parameters, evaluate | CUDA_SITES. They act on whatever Context they are given |
| `alchemy/windows.py::{Parametric,Composed}Hamiltonian.__init__` | derive (inspects forces, serialises) | NON_CUDA_CONTEXT_SITES: no Context |

They are now exercised on CUDA by `tests/test_alchemy_windows_cuda.py` (G1, PASS at `d65d4f0`).
The matrix entries in `afc9d9f` still say "NONE YET"; they should name that lane when S0 lands them.

## The entry point S4 would want `md-run` to call (for S0, request 3)

```python
# md_tools/alchemy/windows.py -- the analogue of stage_main / replica_main / ais_main
def window_main(window: dict[str, Any], argv: list[str] | None = None, *, prepared=None) -> int:
    """Run one fixed-lambda window. `window` is the resolved declaration from resolved.config:
    window_id; the path record (paths.py); every state (samples.ThermodynamicState records);
    WindowSettings; how the Hamiltonian is rebuilt WITHOUT a live Context (the topology-plan
    path and digest, the softcore settings, the restraint record if any); timestep and
    ensemble. argv carries the Amber-like flags: -p -s -c -o -log -r -odir, --cpu, --device,
    --overwrite, --check. There is no -x until a trajectory stream is added."""
```

Contract for it, which `run_window` already follows except where noted:
- It runs `preflight_stage` itself and CONSUMES the result: the platform, the machine settings
  and the loaded System, which it does not re-resolve. `prepared=` lets `md-run` hand over a
  result it has already checked, exactly as `stage_main` does. `run_window(prepared=...)` does
  this; a test makes a re-plan fail.
- The Hamiltonian is rebuilt from the `-s` System and the plan record, and its serialisation is
  compared with `-s` before anything is written. That comparison exists today.
- `--check` returns `report_check(...)` after the read-only continuation validation, and creates
  nothing: `run_window(check=True)`, tested.
- A continuation is validated read-only first: completion manifest, checkpoint fingerprint,
  committed prefix. `validate_public_entry`/`md_tools.run.continuation` should gain a window
  case that calls `validate_stream_prefix` and `verify_window`, so that `md-run` and a
  generated `window.py` refuse identically.
- Serial: a plural launch is refused by `preflight_stage(serial=True)`. Windows run as separate
  processes, and a campaign's provenance is each window's state.
- The output names are `window_paths(out_dir, window_id)`: `.out` and `.log` are two files, and
  the `.log` is the machine record.
